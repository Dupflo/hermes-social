# ── Secrets — never commit these ────────────────────────────────────────────
.env
*.env
!.env.example
!deploy/.env.example
auth.json
*.pem
*.key
secrets/
credentials*

# ── Runtime data (belongs on the VPS volume, not in git) ────────────────────
data/
*.bak
*.log
.deploy-trigger
.pytest_cache/
deploy/docker-compose.override.yml
deploy/meta-webhook-data/

# ── OS / editor noise ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
__pycache__/
*.pyc
node_modules/

# Runtime browser persistence
deploy/camofox-data/
